# Failed Login Investigation

## Objective

The purpose of this investigation is to identify patterns of failed authentication attempts and determine whether they may indicate suspicious activity.

## Useful Command

```bash
grep "Failed password" /var/log/auth.log
